# WEB-005A R3 — Human Visual Review Disposition for `37152222`

**State:** REVISION_REQUIRED / CONTINUATION_AUTHORIZED under existing WEB-005A / MER-107 authority  
**Reviewed implementation:** `feat/web-005a-hero-visual-fidelity@37152222c33fdc09265ba022ce45828c8d002723`  
**Evidence publication checkpoint:** `3087d2515338d9f18089b0f27983dc95c3878ebf`  
**Parent authorities:**  
- `946cd8b5795c86cbeef52f650ee432b8fcd8fd2e:tasks/WEB-005A_R3_FIXED_CAMERA_GLOBE_DRAPE_REVISION.md`  
- `db4605abcb35be9ab0337deb6b15c80aab79b12b:tasks/WEB-005A_R3_ANALYTICAL_ASSET_HANDOFF.md`

This disposition records Product/CTO human visual review of the shipped R3 render plus an actual 1440×900 homepage playback capture. It does not create a new execution start or replace the existing R3/R4 semantics. It identifies the bounded gaps that remain before WEB-005A can return to `REVIEW_READY`.

## 1. Preserve the conforming work

Do not restart the hero from scratch. The reviewed checkpoint contains useful work that should be carried forward unless a conforming implementation requires a local change:

- the generic volumetric EO satellite is readable in the raw shipped render;
- exactly four primary cyan acquisition lines visibly reach the target corners;
- target lock/scan language is materially stronger than R2;
- the post-scan dive/approach reads as one continuous move;
- the Earth/atmosphere treatment, higher-resolution ground detail, provenance work and accepted media ceilings remain useful;
- the live homepage does execute Terrain → THM-01 → ALT-01 → Score in order and reaches a stable final state;
- the post-approach target is in the right-middle hero zone;
- review of the production frames around 231→232 found no visible one-frame seam/pop requiring a repair on its own.

The current checkpoint is rejected for the gaps below, not because these preserved parts must be rebuilt.

## 2. R3 blocker — A-HERO-15 fixed-camera pre-scan is not satisfied

Canonical R3 requires the observer/camera to remain fixed from Earth establish through completion of AOI scan. In the reviewed shipped sequence the Earth grows materially in frame before acquisition, and the implementation record describes the camera as locked only through the acquisition beat rather than from establish through scan.

Continuation requirement:

- re-author the pre-scan choreography so Earth establish → satellite entry/settle → four-corner acquisition → AOI lock/scan occurs with no intentional camera pan/orbit/dolly/zoom/lens animation;
- only after scan completion may the camera begin the approach;
- retain the good satellite/orbit work where possible.

A-HERO-15 must be demonstrated numerically and visually before another full production render.

## 3. Integrated-page acquisition readability — A-HERO-12/13 must pass after compositing

The raw shipped render makes the satellite readable. The actual 1440×900 homepage does not: during the acquisition beat the satellite falls under the left copy/readability treatment and text area, so the four cyan lines appear to originate from an almost invisible platform.

Raw-video readability is therefore insufficient evidence for A-HERO-12/13.

Continuation requirement:

- at 1440-class desktop review width, the integrated homepage must visibly preserve the satellite body/solar-panel silhouette and the line origin throughout the intentional acquisition beat;
- the platform must not sit behind the headline, body copy, CTA or a darkening treatment that destroys the satellite→target relationship;
- preserve the left copy's readability and the Earth-right-dominant hierarchy.

Implementation may solve this through conforming orbit/framing, page compositing/gradient geometry, copy-safe layout, or a bounded combination. Product does not prescribe the edit sequence. Do not weaken the copy merely to make a hidden satellite technically brighter.

## 4. R3/R4 blocker — A-HERO-18 real globe-drape sequence is not satisfied

The reviewed checkpoint uses page-layer raster presentation registered by CSS homography inside a rectangular analysis frame. That is a planar page overlay. It does not implement the canonical DEM-derived relief/globe-surface analytical reveal required by R3/R4.

Continuation must consume the pinned governed assets from the existing handoff authority:

- governed geometry source: `top-dem.tif`;
- display textures: Terrain, THM-01, ALT-01 and Priority from the prepared `webhero_display_final` package;
- one common AOI mapping/UV/geospatial transform for DEM geometry and all four display stages;
- THM-01, ALT-01 and Score remain material overlays and do not deform terrain;
- bounded cinematic DEM vertical exaggeration remains authorized only as presentation geometry per the existing R4 authority.

The analytical sequence must read as terrain/globe surface being analysed in place, not as a flat floating raster/card.

## 5. R3 blocker — A-HERO-19 score-only final hold is not satisfied

The final reviewed state remains a planar analytical surface plus a detached information panel. Canonical R3 requires the final hero payoff to settle on the governed Score layer alone inside the AOI on the globe/relief surface, with earlier Terrain/THM/ALT stages gone.

Continuation requirement:

- end on the registered Score layer draped on the AOI relief/globe surface;
- keep the AOI/frame treatment settled;
- do not use a detached flat raster or detached card as the hero payoff;
- preserve exact public/scientific semantics and required warnings through conforming page treatment; do not recolour or reinterpret governed outputs.

## 6. Mandatory preview gate before another long production render

The previous checkpoint proceeded to a full 276-frame render without demonstrating the canonical R3 steps 6–10 as globe-draped relief. Do not repeat that cost.

Before any new long/full production render, publish low-cost preview/still evidence showing one coherent sequence:

1. fixed-camera Earth establish;
2. satellite left-limb entry and readable settle on the integrated homepage;
3. exactly four lines to the governed AOI corners;
4. AOI lock + scan with camera still fixed;
5. first post-scan approach frame;
6. DEM-derived relief on the AOI/globe surface;
7. THM-01 on the identical registered surface;
8. ALT-01 on the identical registered surface;
9. Score on the identical registered surface;
10. Score-only final hold.

For steps 1–5, include at least one 1440×900 page-composite proof with the real hero copy/readability treatment active, not only an isolated raw render.

Only after these preview gates are visually coherent should implementation spend the full production-render budget.

## 7. Scope and continuation

No new scientific method, palette semantics, AOI footprint, score meaning, provider identity, public claim, media ceiling, deploy/DNS authority or WEB-006 authority is introduced here.

Routine Blender geometry, subdivision, shader/displacement mechanism, UV implementation, orbit solve, camera key derivation, page gradient/compositing, CSS/JS wiring, preview tooling, validator repair and local evidence generation remain implementation-owned inside the existing R3/R4 envelope.

**Continuation state:** E&D may resume MER-107 from this disposition plus the two exact parent authorities above. No additional CTO start is required for this bounded revision. WEB-005A returns to `REVIEW_READY` only after the preview gate is green, the conforming production output is rendered, and the integrated homepage human visual gate is re-submitted.
